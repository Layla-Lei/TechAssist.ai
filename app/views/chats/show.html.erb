<p style="color: green"><%= notice %></p>
<div class="container-fluid">
    <%= link_to 'Go Back to Project', project_path(step: @step.to_i, id: @project.id), :class => 'btn btn-primary', style: 'width: 20%; margin-bottom: 10px; padding: 10px;'  %>
</div>
<%= render @chat %>
