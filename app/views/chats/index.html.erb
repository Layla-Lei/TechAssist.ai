<p style="color: green"><%= notice %></p>
You don't have any chats for this project yet
<%= form_tag chats_path, :method => :post do %> 
  <%= hidden_field_tag 'project_id', @project.id %>
  <%= submit_tag 'Create a chat', :class => 'btn btn-primary btn-block' %>
<% end %>